<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Payment Failed - 1NOTONE</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, #ff6b6b 0%, #ee5a52 50%, #ff4757 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #333;
            overflow-x: hidden;
            position: relative;
        }

        /* Animated background particles */
        .particles {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: 1;
        }

        .particle {
            position: absolute;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 50%;
            animation: float 8s ease-in-out infinite;
        }

        .particle:nth-child(1) { width: 20px; height: 20px; top: 20%; left: 10%; animation-delay: 0s; }
        .particle:nth-child(2) { width: 15px; height: 15px; top: 60%; left: 20%; animation-delay: 1.5s; }
        .particle:nth-child(3) { width: 25px; height: 25px; top: 40%; left: 70%; animation-delay: 3s; }
        .particle:nth-child(4) { width: 18px; height: 18px; top: 80%; left: 80%; animation-delay: 4.5s; }
        .particle:nth-child(5) { width: 22px; height: 22px; top: 30%; left: 90%; animation-delay: 6s; }

        @keyframes float {
            0%, 100% { transform: translateY(0px) rotate(0deg); opacity: 0.3; }
            50% { transform: translateY(-40px) rotate(180deg); opacity: 0.8; }
        }

        .failure-container {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(20px);
            border-radius: 30px;
            padding: 3rem;
            text-align: center;
            box-shadow: 
                0 25px 50px rgba(0, 0, 0, 0.15),
                0 0 0 1px rgba(255, 255, 255, 0.2),
                inset 0 1px 0 rgba(255, 255, 255, 0.3);
            max-width: 850px;
            width: 90%;
            animation: slideInUp 1s cubic-bezier(0.25, 0.46, 0.45, 0.94);
            position: relative;
            z-index: 2;
            border: 1px solid rgba(255, 255, 255, 0.3);
        }

        @keyframes slideInUp {
            from {
                opacity: 0;
                transform: translateY(60px) scale(0.8);
            }
            to {
                opacity: 1;
                transform: translateY(0) scale(1);
            }
        }

        .failure-header {
            position: relative;
            margin-bottom: 2rem;
        }

        .failure-icon-container {
            position: relative;
            display: inline-block;
            margin-bottom: 1.5rem;
        }

        .failure-icon {
            width: 160px;
            height: 160px;
            background: linear-gradient(135deg, #ff6b6b, #ee5a52);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto;
            animation: failurePulse 2s ease-in-out infinite;
            position: relative;
            box-shadow: 
                0 20px 40px rgba(255, 107, 107, 0.3),
                0 0 0 10px rgba(255, 107, 107, 0.1),
                0 0 0 20px rgba(255, 107, 107, 0.05);
        }

        .failure-icon::before {
            content: '';
            position: absolute;
            top: -15px;
            left: -15px;
            right: -15px;
            bottom: -15px;
            background: linear-gradient(135deg, #ff6b6b, #ee5a52);
            border-radius: 50%;
            z-index: -1;
            opacity: 0.2;
            animation: rotate 8s linear infinite reverse;
        }

        .failure-icon::after {
            content: '';
            position: absolute;
            top: -25px;
            left: -25px;
            right: -25px;
            bottom: -25px;
            background: linear-gradient(135deg, #ff6b6b, #ee5a52);
            border-radius: 50%;
            z-index: -2;
            opacity: 0.1;
            animation: rotate 12s linear infinite;
        }

        @keyframes failurePulse {
            0%, 100% { 
                transform: scale(1);
                box-shadow: 
                    0 20px 40px rgba(255, 107, 107, 0.3),
                    0 0 0 10px rgba(255, 107, 107, 0.1),
                    0 0 0 20px rgba(255, 107, 107, 0.05);
            }
            50% { 
                transform: scale(1.05);
                box-shadow: 
                    0 25px 50px rgba(255, 107, 107, 0.4),
                    0 0 0 15px rgba(255, 107, 107, 0.15),
                    0 0 0 30px rgba(255, 107, 107, 0.08);
            }
        }

        @keyframes rotate {
            from { transform: rotate(0deg); }
            to { transform: rotate(360deg); }
        }

        .failure-icon i {
            font-size: 4.5rem;
            color: white;
            z-index: 1;
            position: relative;
            animation: shakeX 1s ease-in-out 0.5s both;
        }

        @keyframes shakeX {
            0%, 100% { transform: translateX(0); }
            10%, 30%, 50%, 70%, 90% { transform: translateX(-5px); }
            20%, 40%, 60%, 80% { transform: translateX(5px); }
        }

        .failure-title {
            font-size: 3.5rem;
            font-weight: 900;
            background: linear-gradient(135deg, #ff6b6b, #ee5a52, #ff4757);
            background-size: 200% 200%;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            margin-bottom: 1rem;
            letter-spacing: -3px;
            animation: gradientShift 3s ease-in-out infinite, titleBounce 1s ease-out 0.8s both;
        }

        @keyframes gradientShift {
            0%, 100% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
        }

        @keyframes titleBounce {
            0% { 
                transform: translateY(30px);
                opacity: 0;
            }
            60% { 
                transform: translateY(-10px);
                opacity: 1;
            }
            100% { 
                transform: translateY(0);
                opacity: 1;
            }
        }

        .failure-subtitle {
            font-size: 1.3rem;
            color: #666;
            margin-bottom: 2.5rem;
            line-height: 1.6;
            font-weight: 500;
            animation: fadeInUp 1s ease-out 1s both;
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .error-section {
            background: linear-gradient(135deg, rgba(255, 107, 107, 0.08), rgba(238, 90, 82, 0.08));
            border-radius: 25px;
            padding: 2.5rem;
            margin: 2.5rem 0;
            border: 2px solid rgba(255, 107, 107, 0.1);
            position: relative;
            overflow: hidden;
            animation: slideInLeft 1s ease-out 1.2s both;
            text-align: left;
        }

        @keyframes slideInLeft {
            from {
                opacity: 0;
                transform: translateX(-50px);
            }
            to {
                opacity: 1;
                transform: translateX(0);
            }
        }

        .error-section::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: linear-gradient(90deg, #ff6b6b, #ee5a52, #ff4757);
            background-size: 200% 100%;
            animation: gradientFlow 2s ease-in-out infinite;
        }

        @keyframes gradientFlow {
            0%, 100% { background-position: 0% 0%; }
            50% { background-position: 100% 0%; }
        }

        .error-section h3 {
            color: #333;
            margin-bottom: 1.5rem;
            font-size: 1.6rem;
            font-weight: 700;
            display: flex;
            align-items: center;
            gap: 0.8rem;
        }

        .error-details {
            background: rgba(255, 255, 255, 0.8);
            border-radius: 18px;
            padding: 1.5rem;
            color: #555;
            font-weight: 500;
            border: 1px solid rgba(0, 0, 0, 0.05);
            font-size: 1.1rem;
            line-height: 1.6;
        }

        .help-section {
            background: linear-gradient(135deg, rgba(52, 152, 219, 0.08), rgba(41, 128, 185, 0.08));
            border-radius: 25px;
            padding: 2.5rem;
            margin: 2.5rem 0;
            border: 2px solid rgba(52, 152, 219, 0.1);
            position: relative;
            overflow: hidden;
            animation: slideInRight 1s ease-out 1.4s both;
            text-align: left;
        }

        @keyframes slideInRight {
            from {
                opacity: 0;
                transform: translateX(50px);
            }
            to {
                opacity: 1;
                transform: translateX(0);
            }
        }

        .help-section::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: linear-gradient(90deg, #3498db, #2980b9, #3498db);
            background-size: 200% 100%;
            animation: gradientFlow 2s ease-in-out infinite;
        }

        .help-section h4 {
            color: #3498db;
            margin-bottom: 2rem;
            display: flex;
            align-items: center;
            gap: 0.8rem;
            font-weight: 700;
            font-size: 1.4rem;
        }

        .help-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 1.5rem;
        }

        .help-item {
            background: rgba(255, 255, 255, 0.8);
            border-radius: 18px;
            padding: 1.5rem;
            transition: all 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
            border: 1px solid rgba(0, 0, 0, 0.05);
            position: relative;
            overflow: hidden;
        }

        .help-item::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(52, 152, 219, 0.1), transparent);
            transition: left 0.6s ease;
        }

        .help-item:hover {
            transform: translateY(-5px) scale(1.02);
            box-shadow: 0 15px 35px rgba(52, 152, 219, 0.15);
        }

        .help-item:hover::before {
            left: 100%;
        }

        .help-item-icon {
            width: 50px;
            height: 50px;
            background: linear-gradient(135deg, #3498db, #2980b9);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 1.2rem;
            margin-bottom: 1rem;
        }

        .help-item h5 {
            color: #333;
            font-weight: 600;
            margin-bottom: 0.8rem;
            font-size: 1.1rem;
        }

        .help-item p {
            color: #666;
            font-size: 0.95rem;
            line-height: 1.5;
            margin: 0;
        }

        .contact-section {
            background: linear-gradient(135deg, rgba(255, 193, 7, 0.08), rgba(255, 152, 0, 0.08));
            border-radius: 25px;
            padding: 2.5rem;
            margin: 2.5rem 0;
            border: 2px solid rgba(255, 193, 7, 0.1);
            position: relative;
            overflow: hidden;
            animation: fadeInUp 1s ease-out 1.6s both;
        }

        .contact-section::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: linear-gradient(90deg, #ffc107, #ff9800, #ffc107);
            background-size: 200% 100%;
            animation: gradientFlow 2s ease-in-out infinite;
        }

        .contact-section h4 {
            color: #ff9800;
            margin-bottom: 2rem;
            display: flex;
            align-items: center;
            gap: 0.8rem;
            font-weight: 700;
            font-size: 1.4rem;
        }

        .contact-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 1.5rem;
            text-align: left;
        }

        .contact-item {
            background: rgba(255, 255, 255, 0.8);
            border-radius: 18px;
            padding: 1.5rem;
            display: flex;
            align-items: center;
            gap: 1rem;
            transition: all 0.3s ease;
            border: 1px solid rgba(0, 0, 0, 0.05);
        }

        .contact-item:hover {
            transform: translateY(-3px);
            box-shadow: 0 10px 25px rgba(255, 152, 0, 0.15);
        }

        .contact-icon {
            width: 50px;
            height: 50px;
            background: linear-gradient(135deg, #ffc107, #ff9800);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 1.2rem;
            flex-shrink: 0;
        }

        .contact-info h5 {
            color: #333;
            font-weight: 600;
            margin-bottom: 0.3rem;
            font-size: 1rem;
        }

        .contact-info p {
            color: #666;
            font-size: 0.9rem;
            margin: 0;
        }

        .action-buttons {
            display: flex;
            gap: 1.5rem;
            justify-content: center;
            margin-top: 3rem;
            flex-wrap: wrap;
            animation: fadeInUp 1s ease-out 1.8s both;
        }

        .btn {
            padding: 18px 36px;
            border: none;
            border-radius: 50px;
            font-size: 1.1rem;
            font-weight: 600;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            gap: 0.8rem;
            transition: all 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
            cursor: pointer;
            position: relative;
            overflow: hidden;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.3), transparent);
            transition: left 0.6s ease;
        }

        .btn:hover::before {
            left: 100%;
        }

        .btn-danger {
            background: linear-gradient(135deg, #ff6b6b, #ee5a52);
            color: white;
            box-shadow: 0 15px 35px rgba(255, 107, 107, 0.3);
        }

        .btn-danger:hover {
            transform: translateY(-5px) scale(1.05);
            box-shadow: 0 20px 45px rgba(255, 107, 107, 0.4);
        }

        .btn-primary {
            background: linear-gradient(135deg, #3498db, #2980b9);
            color: white;
            box-shadow: 0 15px 35px rgba(52, 152, 219, 0.3);
        }

        .btn-primary:hover {
            transform: translateY(-5px) scale(1.05);
            box-shadow: 0 20px 45px rgba(52, 152, 219, 0.4);
        }

        .btn-secondary {
            background: linear-gradient(135deg, #95a5a6, #7f8c8d);
            color: white;
            box-shadow: 0 15px 35px rgba(149, 165, 166, 0.3);
        }

        .btn-secondary:hover {
            transform: translateY(-5px) scale(1.05);
            box-shadow: 0 20px 45px rgba(149, 165, 166, 0.4);
        }

        /* Enhanced Responsive Design */
        @media (max-width: 1024px) {
            .failure-container {
                max-width: 90%;
                padding: 2.5rem;
                margin: 2rem auto;
            }

            .failure-title {
                font-size: 3rem;
                letter-spacing: -2px;
            }

            .failure-icon {
                width: 140px;
                height: 140px;
            }

            .failure-icon i {
                font-size: 4rem;
            }

            .help-grid {
                grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
                gap: 1.25rem;
            }

            .contact-grid {
                grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
                gap: 1.25rem;
            }
        }

        @media (max-width: 768px) {
            body {
                padding: 1rem 0;
                min-height: auto;
            }

            .failure-container {
                padding: 1.5rem;
                margin: 0.5rem;
                border-radius: 20px;
                width: calc(100% - 1rem);
                max-width: none;
            }

            .failure-title {
                font-size: 2.5rem;
                letter-spacing: -1px;
                margin-bottom: 0.75rem;
            }

            .failure-subtitle {
                font-size: 1.1rem;
                margin-bottom: 2rem;
                line-height: 1.5;
            }

            .failure-icon {
                width: 120px;
                height: 120px;
                margin-bottom: 1rem;
            }

            .failure-icon i {
                font-size: 3rem;
            }

            .failure-icon::before,
            .failure-icon::after {
                top: -10px;
                left: -10px;
                right: -10px;
                bottom: -10px;
            }

            .failure-icon::after {
                top: -15px;
                left: -15px;
                right: -15px;
                bottom: -15px;
            }

            .error-section,
            .help-section,
            .contact-section {
                padding: 1.5rem;
                margin: 1.5rem 0;
                border-radius: 15px;
            }

            .error-section h3,
            .help-section h4,
            .contact-section h4 {
                font-size: 1.2rem;
                margin-bottom: 1rem;
                flex-direction: column;
                align-items: flex-start;
                gap: 0.5rem;
            }

            .error-details {
                padding: 1rem;
                border-radius: 12px;
                font-size: 1rem;
                line-height: 1.5;
            }

            .help-grid,
            .contact-grid {
                grid-template-columns: 1fr;
                gap: 1rem;
            }

            .help-item,
            .contact-item {
                padding: 1rem;
                border-radius: 12px;
            }

            .help-item-icon,
            .contact-icon {
                width: 40px;
                height: 40px;
                font-size: 1rem;
                margin-bottom: 0.75rem;
            }

            .contact-item {
                flex-direction: column;
                align-items: flex-start;
                text-align: left;
                gap: 0.75rem;
            }

            .contact-icon {
                margin-bottom: 0;
            }

            .help-item h5,
            .contact-info h5 {
                font-size: 1rem;
                margin-bottom: 0.5rem;
            }

            .help-item p,
            .contact-info p {
                font-size: 0.9rem;
                line-height: 1.4;
            }

            .action-buttons {
                flex-direction: column;
                align-items: stretch;
                gap: 1rem;
                margin-top: 2rem;
            }

            .btn {
                width: 100%;
                justify-content: center;
                padding: 1rem 1.5rem;
                font-size: 1rem;
                border-radius: 12px;
                max-width: none;
            }
        }

        @media (max-width: 480px) {
            body {
                padding: 0.5rem 0;
            }

            .failure-container {
                padding: 1rem;
                margin: 0.25rem;
                border-radius: 15px;
                width: calc(100% - 0.5rem);
            }

            .failure-title {
                font-size: 2rem;
                letter-spacing: -1px;
                margin-bottom: 0.5rem;
            }

            .failure-subtitle {
                font-size: 1rem;
                margin-bottom: 1.5rem;
                line-height: 1.4;
            }

            .failure-icon {
                width: 100px;
                height: 100px;
                margin-bottom: 0.75rem;
            }

            .failure-icon i {
                font-size: 2.5rem;
            }

            .failure-icon::before {
                top: -8px;
                left: -8px;
                right: -8px;
                bottom: -8px;
            }

            .failure-icon::after {
                top: -12px;
                left: -12px;
                right: -12px;
                bottom: -12px;
            }

            .error-section,
            .help-section,
            .contact-section {
                padding: 1rem;
                margin: 1rem 0;
                border-radius: 12px;
            }

            .error-section h3,
            .help-section h4,
            .contact-section h4 {
                font-size: 1.1rem;
                margin-bottom: 0.75rem;
                gap: 0.375rem;
            }

            .error-details {
                padding: 0.75rem;
                border-radius: 8px;
                font-size: 0.9rem;
                line-height: 1.4;
            }

            .help-grid,
            .contact-grid {
                gap: 0.75rem;
            }

            .help-item,
            .contact-item {
                padding: 0.75rem;
                border-radius: 8px;
            }

            .help-item-icon,
            .contact-icon {
                width: 35px;
                height: 35px;
                font-size: 0.9rem;
                margin-bottom: 0.5rem;
            }

            .contact-item {
                gap: 0.5rem;
            }

            .help-item h5,
            .contact-info h5 {
                font-size: 0.9rem;
                margin-bottom: 0.375rem;
            }

            .help-item p,
            .contact-info p {
                font-size: 0.8rem;
                line-height: 1.3;
            }

            .action-buttons {
                gap: 0.75rem;
                margin-top: 1.5rem;
            }

            .btn {
                padding: 0.875rem 1rem;
                font-size: 0.9rem;
                border-radius: 10px;
                letter-spacing: 0.5px;
            }

            .btn i {
                font-size: 0.9rem;
            }
        }

        @media (max-width: 360px) {
            .failure-container {
                padding: 0.75rem;
                margin: 0.125rem;
                width: calc(100% - 0.25rem);
            }

            .failure-title {
                font-size: 1.75rem;
                margin-bottom: 0.375rem;
            }

            .failure-subtitle {
                font-size: 0.9rem;
                margin-bottom: 1.25rem;
            }

            .failure-icon {
                width: 80px;
                height: 80px;
                margin-bottom: 0.5rem;
            }

            .failure-icon i {
                font-size: 2rem;
            }

            .error-section,
            .help-section,
            .contact-section {
                padding: 0.75rem;
                margin: 0.75rem 0;
            }

            .error-section h3,
            .help-section h4,
            .contact-section h4 {
                font-size: 1rem;
                margin-bottom: 0.5rem;
            }

            .error-details {
                padding: 0.625rem;
                font-size: 0.85rem;
            }

            .help-item,
            .contact-item {
                padding: 0.625rem;
            }

            .help-item-icon,
            .contact-icon {
                width: 30px;
                height: 30px;
                font-size: 0.8rem;
            }

            .help-item h5,
            .contact-info h5 {
                font-size: 0.85rem;
            }

            .help-item p,
            .contact-info p {
                font-size: 0.75rem;
            }

            .btn {
                padding: 0.75rem 0.875rem;
                font-size: 0.85rem;
                letter-spacing: 0.25px;
            }
        }

        /* Landscape orientation adjustments for mobile */
        @media (max-width: 768px) and (orientation: landscape) {
            body {
                padding: 0.5rem 0;
            }

            .failure-container {
                margin: 0.5rem auto;
                max-height: 90vh;
                overflow-y: auto;
            }

            .failure-icon {
                width: 80px;
                height: 80px;
                margin-bottom: 0.5rem;
            }

            .failure-icon i {
                font-size: 2rem;
            }

            .failure-title {
                font-size: 2rem;
                margin-bottom: 0.5rem;
            }

            .failure-subtitle {
                font-size: 0.9rem;
                margin-bottom: 1rem;
            }

            .error-section,
            .help-section,
            .contact-section {
                padding: 1rem;
                margin: 1rem 0;
            }

            .help-grid {
                grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
                gap: 0.75rem;
            }

            .contact-grid {
                grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
                gap: 0.75rem;
            }

            .action-buttons {
                flex-direction: row;
                flex-wrap: wrap;
                justify-content: center;
                gap: 0.75rem;
                margin-top: 1rem;
            }

            .btn {
                flex: 1;
                min-width: 140px;
                max-width: 200px;
                padding: 0.75rem 1rem;
                font-size: 0.85rem;
            }
        }

        /* Touch-friendly improvements */
        @media (max-width: 768px) {
            .btn {
                min-height: 44px; /* Minimum touch target size */
                touch-action: manipulation;
                -webkit-tap-highlight-color: transparent;
            }

            .help-item,
            .contact-item {
                min-height: 44px;
                touch-action: manipulation;
            }

            /* Improve text readability on small screens */
            .failure-subtitle,
            .error-details,
            .help-item p,
            .contact-info p {
                -webkit-font-smoothing: antialiased;
                -moz-osx-font-smoothing: grayscale;
            }
        }

        /* High DPI display adjustments */
        @media (-webkit-min-device-pixel-ratio: 2), (min-resolution: 192dpi) {
            .failure-icon,
            .help-item-icon,
            .contact-icon {
                -webkit-font-smoothing: antialiased;
                -moz-osx-font-smoothing: grayscale;
            }
        }
    </style>
</head>
<body>
    <div class="particles">
        <div class="particle"></div>
        <div class="particle"></div>
        <div class="particle"></div>
        <div class="particle"></div>
        <div class="particle"></div>
    </div>

    <div class="failure-container">
        <div class="failure-header">
            <div class="failure-icon-container">
                <div class="failure-icon">
                    <i class="fas fa-times"></i>
                </div>
            </div>
            <h1 class="failure-title">Payment Failed!</h1>
            <p class="failure-subtitle">
                😔 We're sorry, but your payment could not be processed. 
                Don't worry - we're here to help you complete your purchase!
            </p>
        </div>

        <% if (typeof error !== 'undefined' && error) { %>
        <div class="error-section">
            <h3><i class="fas fa-exclamation-triangle"></i> What went wrong?</h3>
            <div class="error-details">
                <%= error %>
            </div>
        </div>
        <% } %>

        <div class="help-section">
            <h4><i class="fas fa-lightbulb"></i> Quick Solutions</h4>
            <div class="help-grid">
                <div class="help-item">
                    <div class="help-item-icon">
                        <i class="fas fa-wifi"></i>
                    </div>
                    <h5>Check Connection</h5>
                    <p>Ensure you have a stable internet connection and try again</p>
                </div>
                <div class="help-item">
                    <div class="help-item-icon">
                        <i class="fas fa-credit-card"></i>
                    </div>
                    <h5>Verify Card Details</h5>
                    <p>Double-check your card number, expiry date, and CVV</p>
                </div>
                <div class="help-item">
                    <div class="help-item-icon">
                        <i class="fas fa-wallet"></i>
                    </div>
                    <h5>Check Balance</h5>
                    <p>Make sure you have sufficient funds in your account</p>
                </div>
                <div class="help-item">
                    <div class="help-item-icon">
                        <i class="fas fa-exchange-alt"></i>
                    </div>
                    <h5>Try Different Method</h5>
                    <p>Use another card, UPI, or Cash on Delivery option</p>
                </div>
                <div class="help-item">
                    <div class="help-item-icon">
                        <i class="fas fa-phone"></i>
                    </div>
                    <h5>Contact Bank</h5>
                    <p>If the issue persists, contact your bank for assistance</p>
                </div>
                <div class="help-item">
                    <div class="help-item-icon">
                        <i class="fas fa-truck"></i>
                    </div>
                    <h5>Cash on Delivery</h5>
                    <p>Choose COD as a safe alternative payment method</p>
                </div>
            </div>
        </div>

        <div class="contact-section">
            <h4><i class="fas fa-headset"></i> Need Personal Assistance?</h4>
            <div class="contact-grid">
                <div class="contact-item">
                    <div class="contact-icon">
                        <i class="fas fa-envelope"></i>
                    </div>
                    <div class="contact-info">
                        <h5>Email Support</h5>
                        <p>support@1notone.com</p>
                    </div>
                </div>
                <div class="contact-item">
                    <div class="contact-icon">
                        <i class="fas fa-phone"></i>
                    </div>
                    <div class="contact-info">
                        <h5>Phone Support</h5>
                        <p>+91-XXXXXXXXXX (9 AM - 9 PM)</p>
                    </div>
                </div>
                <div class="contact-item">
                    <div class="contact-icon">
                        <i class="fas fa-comments"></i>
                    </div>
                    <div class="contact-info">
                        <h5>Live Chat</h5>
                        <p>Available on our website</p>
                    </div>
                </div>
            </div>
        </div>

        <div class="action-buttons">
            <% if (typeof failedOrderId !== 'undefined' && failedOrderId) { %>
            <a href="/checkout/retry-payment/<%= failedOrderId %>" class="btn btn-danger">
                <i class="fas fa-redo"></i>
                Retry Payment
            </a>
            <a href="/orders/details/<%= failedOrderId %>" class="btn btn-primary">
                <i class="fas fa-receipt"></i>
                View Order
            </a>
            <% } else { %>
            <button id="retryPaymentBtn" class="btn btn-danger">
                <i class="fas fa-redo"></i>
                Retry Payment
            </button>
            <% } %>
            <a href="/cart" class="btn btn-primary">
                <i class="fas fa-shopping-cart"></i>
                Back to Cart
            </a>
            <a href="/shop" class="btn btn-secondary">
                <i class="fas fa-shopping-bag"></i>
                Continue Shopping
            </a>
        </div>
    </div>

    <script src="https://checkout.razorpay.com/v1/checkout.js"></script>
    <script>
        // Get error from URL parameters
        const urlParams = new URLSearchParams(window.location.search);
        const errorMessage = urlParams.get('error');
        const failedOrderId = urlParams.get('orderId');
        
        if (errorMessage && !document.querySelector('.error-section')) {
            const errorDiv = document.createElement('div');
            errorDiv.className = 'error-section';
            errorDiv.innerHTML = `
                <h3><i class="fas fa-exclamation-triangle"></i> What went wrong?</h3>
                <div class="error-details">${errorMessage}</div>
            `;
            
            const failureSubtitle = document.querySelector('.failure-subtitle');
            failureSubtitle.parentNode.insertBefore(errorDiv, failureSubtitle.nextSibling);
        }

        // Add "View Order" button if failedOrderId is available in URL and button doesn't exist
        if (failedOrderId && !document.querySelector('a[href*="/orders/details/"]')) {
            const actionButtons = document.querySelector('.action-buttons');
            const retryButton = document.getElementById('retryPaymentBtn');
            
            const viewOrderButton = document.createElement('a');
            viewOrderButton.href = `/orders/details/${failedOrderId}`;
            viewOrderButton.className = 'btn btn-primary';
            viewOrderButton.innerHTML = `
                <i class="fas fa-receipt"></i>
                View Order
            `;
            
            // Insert the View Order button after the Retry Payment button
            if (retryButton && retryButton.nextSibling) {
                actionButtons.insertBefore(viewOrderButton, retryButton.nextSibling);
            } else if (retryButton) {
                retryButton.parentNode.insertBefore(viewOrderButton, retryButton.nextSibling);
            } else {
                actionButtons.insertBefore(viewOrderButton, actionButtons.firstChild);
            }
        }

        // Retry payment functionality - opens Razorpay directly
        const retryBtn = document.getElementById('retryPaymentBtn');
        if (retryBtn) {
            retryBtn.addEventListener('click', async function() {
                try {
                    this.disabled = true;
                    this.innerHTML = '<i class="fas fa-spinner fa-spin"></i> Processing...';

                    // Get retry payment order details
                    const response = await fetch('/checkout/retry-payment', {
                        method: 'POST',
                        headers: {
                            'Content-Type': 'application/json'
                        },
                        body: JSON.stringify({
                            failedOrderId: failedOrderId
                        })
                    });

                    const result = await response.json();

                    if (!result.success) {
                        throw new Error(result.message || 'Failed to initiate payment');
                    }

                    // Initialize Razorpay directly on this page
                    const options = {
                        key: result.key,
                        amount: result.amount,
                        currency: result.currency,
                        name: '1NOTONE',
                        description: 'Retry Payment',
                        order_id: result.orderId,
                        handler: async function(response) {
                            try {
                                // Verify payment
                                const verifyResponse = await fetch('/checkout/verify-payment', {
                                    method: 'POST',
                                    headers: {
                                        'Content-Type': 'application/json'
                                    },
                                    body: JSON.stringify({
                                        razorpay_order_id: response.razorpay_order_id,
                                        razorpay_payment_id: response.razorpay_payment_id,
                                        razorpay_signature: response.razorpay_signature,
                                        addressId: result.addressId
                                    })
                                });

                                const verifyResult = await verifyResponse.json();
                                console.log('Payment verification result:', verifyResult);

                                if (verifyResult.success) {
                                    console.log('Payment verified successfully, redirecting to:', verifyResult.redirectUrl);
                                    // Redirect to success page
                                    if (verifyResult.redirectUrl) {
                                        window.location.href = verifyResult.redirectUrl;
                                    } else {
                                        console.error('No redirect URL provided in response');
                                        window.location.href = `/checkout/payment-success/${verifyResult.orderId}`;
                                    }
                                } else {
                                    console.error('Payment verification failed:', verifyResult.message);
                                    throw new Error(verifyResult.message || 'Payment verification failed');
                                }
                            } catch (error) {
                                console.error('Payment verification error:', error);
                                alert('Payment verification failed. Please contact support.');
                            }
                        },
                        modal: {
                            ondismiss: function() {
                                // Re-enable button when modal is dismissed
                                document.getElementById('retryPaymentBtn').disabled = false;
                                document.getElementById('retryPaymentBtn').innerHTML = '<i class="fas fa-redo"></i> Retry Payment';
                            }
                        },
                        theme: {
                            color: '#ff6b6b'
                        }
                    };

                    const rzp = new Razorpay(options);
                    rzp.on('payment.failed', function(response) {
                        console.error('Payment failed:', response.error);
                        // Stay on the same page and show the error
                        window.location.href = `/checkout/payment-failure?error=${encodeURIComponent(response.error.description || 'Payment failed')}`;
                    });

                    rzp.open();

                } catch (error) {
                    console.error('Error initiating retry payment:', error);
                    alert(error.message || 'Failed to initiate payment. Please try again.');
                    this.disabled = false;
                    this.innerHTML = '<i class="fas fa-redo"></i> Retry Payment';
                }
            });
        }

        // Add hover effects to help items
        document.querySelectorAll('.help-item').forEach(item => {
            item.addEventListener('mouseenter', function() {
                this.style.transform = 'translateY(-5px) scale(1.02)';
            });
            
            item.addEventListener('mouseleave', function() {
                this.style.transform = 'translateY(0) scale(1)';
            });
        });

        // Add click animation to buttons
        document.querySelectorAll('.btn').forEach(button => {
            button.addEventListener('click', function(e) {
                const ripple = document.createElement('span');
                const rect = this.getBoundingClientRect();
                const size = Math.max(rect.width, rect.height);
                const x = e.clientX - rect.left - size / 2;
                const y = e.clientY - rect.top - size / 2;
                
                ripple.style.width = ripple.style.height = size + 'px';
                ripple.style.left = x + 'px';
                ripple.style.top = y + 'px';
                ripple.style.position = 'absolute';
                ripple.style.borderRadius = '50%';
                ripple.style.background = 'rgba(255, 255, 255, 0.5)';
                ripple.style.transform = 'scale(0)';
                ripple.style.animation = 'ripple 0.6s linear';
                ripple.style.pointerEvents = 'none';
                
                this.appendChild(ripple);
                
                setTimeout(() => {
                    ripple.remove();
                }, 600);
            });
        });

        // Add ripple animation CSS
        const style = document.createElement('style');
        style.textContent = `
            @keyframes ripple {
                to {
                    transform: scale(4);
                    opacity: 0;
                }
            }
        `;
        document.head.appendChild(style);

        // Auto-focus on retry button for better UX
        window.addEventListener('load', () => {
            const retryButton = document.querySelector('#retryPaymentBtn, .btn-danger');
            if (retryButton) {
                retryButton.focus();
            }
        });
    </script>
</body>
</html>